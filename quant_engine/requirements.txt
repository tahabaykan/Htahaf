# Core dependencies
ib_insync>=0.9.70
nest_asyncio>=1.5.0  # IBKR connect thread (nibkrtry-style), Windows event loop
requests>=2.28.0  # RevnBookCheck / terminals HTTP to backend
fastapi>=0.104.1
python-multipart>=0.0.6
uvicorn[standard]>=0.24.0
redis>=5.0.0
pydantic>=2.5.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# Async support (redis-py 5.0+ has built-in async support)
# aioredis>=2.0.1  # Removed - incompatible with Python 3.13
# asyncio  # Built-in module, no need to install

# Logging
loguru>=0.7.2

# Optional: Monitoring
prometheus-client>=0.19.0

# Optional: WebSocket
# Python 3.14: websockets legacy server raises "Timeout should be used inside a task".
# Use wsproto for uvicorn WebSocket (see main.py run_api ws="wsproto").
websockets>=12.0
wsproto>=1.2.0  # WebSocket protocol for uvicorn (avoids websockets legacy server on Python 3.14)
websocket-client>=1.6.0  # For Hammer Pro WebSocket client

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
psutil>=5.9.0
pyyaml>=6.0

# Backtesting
pandas>=2.0.0
pyarrow>=14.0.0

# Optimization
python-dateutil>=2.8.2
pytz>=2023.3  # For timezone handling (NYSE trading calendar)

# Reporting
matplotlib>=3.7.0
seaborn>=0.12.0
jinja2>=3.1.0

# Advanced Optimization
optuna>=3.4.0
plotly>=5.17.0
kaleido>=0.2.1  # For plotly image export

# Monte Carlo Simulation
joblib>=1.3.0
numpy>=1.24.0

